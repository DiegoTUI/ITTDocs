type: object
description: Could not perform migration because it's blocked.
properties:
  code:
    type: int32
    description: Error code
    example: 18
  name:
    type: string
    description: Error name
    example: MigrationBlockedError
  message:
    type: string
    description: Error message
    example: Not possible to perform migration. There is another migration in-flight.
  httpStatus:
    type: integer
    format: int32
    description: HTTP status code
    example: 500